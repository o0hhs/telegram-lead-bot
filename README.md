# 🤖 Telegram-бот для бизнеса (Showcase)

> Готовое решение для приёма заявок от клиентов через Telegram

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![aiogram](https://img.shields.io/badge/aiogram-3.x-green.svg)](https://github.com/aiogram/aiogram)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

## 📋 Описание

Простой и функциональный Telegram-бот для сбора заявок от клиентов. Идеально подходит для малого бизнеса, фрилансеров и стартапов.

## ✨ Возможности

- 📝 **Форма обратной связи** — сбор имени, телефона и сообщения
- 🔔 **Уведомления администратору** — моментальное оповещение о новых заявках
- 💾 **Сохранение данных** — все заявки сохраняются в файл `leads.txt`
- ℹ️ **Информационные разделы** — о компании, контакты, FAQ
- ✅ **Валидация данных** — проверка корректности ввода
- ❌ **Отмена действий** — возможность отменить заполнение формы
- 🎨 **Красивые кнопки** — интуитивный интерфейс

## 🛠 Технологии

- **Python 3.11+**
- **aiogram 3.x** — асинхронный фреймворк для Telegram Bot API
- **python-dotenv** — управление переменными окружения
- **asyncio** — асинхронное программирование

## 🚀 Установка и запуск

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/yourusername/business-bot.git
cd business-bot
```

### 2. Создайте виртуальное окружение
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установите зависимости
```bash
pip install aiogram python-dotenv
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:
```env
TOKEN=ваш_токен_бота_от_BotFather
```

### 5. Укажите ID администратора

В файле `main.py` найдите строку:
```python
ADMIN_ID = 123456789  # ЗАМЕНИТЕ НА СВОЙ ID
```

Замените на свой Telegram ID (узнать можно у бота [@userinfobot](https://t.me/userinfobot))

### 6. Запустите бота
```bash
python main.py
```

## 💬 Команды бота

- `/start` — Запустить бота и показать главное меню
- `/help` — Справка по командам
- `/cancel` — Отменить текущее действие

## 🎯 Основные функции

### Форма обратной связи
1. Пользователь нажимает "📝 Оставить заявку"
2. Вводит своё имя
3. Указывает номер телефона
4. Описывает свой вопрос или потребность
5. Получает подтверждение

### Уведомления администратора
После отправки заявки администратор получает сообщение с:
- Именем клиента
- Номером телефона
- Текстом сообщения
- Username в Telegram
- Временем создания заявки

### Сохранение данных
Все заявки автоматически сохраняются в файл `leads.txt` в формате:
```
========================================
НОВАЯ ЗАЯВКА - 2024-01-15 14:30:25
========================================
Имя: Иван Иванов
Телефон: +79991234567
Сообщение: Интересует услуга...
Telegram: @ivan_user
User ID: 123456789
========================================
```

## 📝 Кастомизация

### Изменение текста

Все тексты находятся в соответствующих функциях. Например, для изменения приветствия отредактируйте функцию `start()`:

```python
@dp.message(CommandStart())
async def start(message: Message):
    await message.answer(
        f"👋 Добро пожаловать в ВАШУ КОМПАНИЮ...",
        # ...
    )
```

### Добавление полей в форму

Чтобы добавить новое поле (например, email):

1. Добавьте состояние в класс `FeedbackForm`:
```python
class FeedbackForm(StatesGroup):
    name = State()
    phone = State()
    email = State()  # новое поле
    message = State()
```

2. Создайте обработчик:
```python
@dp.message(FeedbackForm.email)
async def process_email(message: Message, state: FSMContext):
    await state.update_data(email=message.text)
    await state.set_state(FeedbackForm.message)
    # ...
```

### Интеграция с базой данных

Вместо файла можно использовать SQLite или PostgreSQL:

```python
# Пример с SQLite
import sqlite3

def save_lead(name, phone, message, telegram_id):
    conn = sqlite3.connect('leads.db')
    cursor = conn.cursor()
    cursor.execute('''
        INSERT INTO leads (name, phone, message, telegram_id, created_at)
        VALUES (?, ?, ?, ?, datetime('now'))
    ''', (name, phone, message, telegram_id))
    conn.commit()
    conn.close()
```

## 📊 Примеры использования

### Для интернет-магазина
Измените вопросы формы на:
- Какой товар интересует?
- Количество
- Способ доставки

### Для консультаций
Добавьте:
- Тип услуги (из списка)
- Удобное время звонка
- Откуда узнали о нас

### Для записи на услуги
Добавьте:
- Выбор услуги
- Желаемую дату
- Желаемое время

## 🔧 Требования

- Python 3.11 или выше
- aiogram 3.x
- python-dotenv

## 📄 Структура проекта

```
business-bot/
├── main.py              # Основной файл бота
├── .env                 # Переменные окружения (не в git)
├── leads.txt            # Сохранённые заявки (создаётся автоматически)
├── requirements.txt     # Зависимости
└── README.md           # Документация
```

## 🐛 Решение проблем

**Бот не отвечает:**
- Проверьте правильность токена в `.env`
- Убедитесь, что бот запущен

**Не приходят уведомления администратору:**
- Проверьте, что указан правильный `ADMIN_ID`
- Попробуйте сначала написать боту от имени администратора

**Ошибка при сохранении в файл:**
- Убедитесь, что у программы есть права на запись
- Проверьте наличие свободного места на диске

## 📞 Контакты

**Автор:** o0hhs
- Telegram: [@o0hhs](https://t.me/o0hhs)
- GitHub: [github.com/o0hhs](https://github.com/o0hhs)

## 📜 Лицензия

MIT License - можете свободно использовать в коммерческих проектах.

---

⭐ **Если проект был полезен — поставьте звёздочку!**

💼 **Нужна доработка под ваш бизнес?** Пишите в Telegram!
